% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xl_table.R
\name{xl_table_column}
\alias{xl_table_column}
\title{Describe a column of a worksheet table}
\usage{
xl_table_column(
  col,
  header = NULL,
  formula = NULL,
  total = NULL,
  total_label = NULL,
  format = NULL,
  header_format = NULL
)
}
\arguments{
\item{col}{The column: a name or a 1-based position within the table's range.}

\item{header}{The header caption.  Defaults to the data frame's column name
--- which is also what makes a table safe to add, since Excel rejects a
file whose table definition and header cells disagree.}

\item{formula}{A formula filling every data cell of the column, usually with
a structured reference such as `"=SUM(Sales[@[Q1]:[Q4]])"`.  Because those
name the table, see the `name` argument of [xl_table()].}

\item{total}{The function shown in this column's total-row cell: one of
`"sum"`, `"average"`, `"count"`, `"count_nums"`, `"max"`, `"min"`,
`"std_dev"` or `"var"`.  Needs `total_row = TRUE` on the table.}

\item{total_label}{A string for the total-row cell instead of a function ---
typically `"Total"` under the first column.}

\item{format}{An [xl_format] applied to the column's data cells.}

\item{header_format}{An [xl_format] applied to the column's header cell.}
}
\value{
An `xl_table_column` object.
}
\description{
`xl_table_column()` overrides what [xl_table()] does with one column: its
header caption, a formula filling the column, what its total-row cell shows,
and the formats applied to the header and the data cells.

Only the columns you name need an entry; the rest take the data frame's
column name as their header and are left otherwise alone.
}
\examples{
xl_table_column("qty", total = "sum")
xl_table_column("fruit", total_label = "Total")
xl_table_column("margin", formula = "=Sales[@revenue] * 0.3")
}
\seealso{
[xl_table], [xl_sheet]

Other writexl:
\code{\link[=is_xl_comment]{is_xl_comment()}},
\code{\link[=is_xl_format]{is_xl_format()}},
\code{\link[=xl_cell_general]{xl_cell_general()}},
\code{\link[=xl_color]{xl_color()}},
\code{\link{xl_colrow_spec}},
\code{\link[=xl_comment]{xl_comment()}},
\code{\link{xl_conditional}},
\code{\link[=xl_filter]{xl_filter()}},
\code{\link[=xl_filter_keep]{xl_filter_keep()}},
\code{\link[=xl_format]{xl_format()}},
\code{\link{xl_format_groups}},
\code{\link[=xl_formula]{xl_formula()}},
\code{\link[=xl_merge]{xl_merge()}},
\code{\link[=xl_outline]{xl_outline()}},
\code{\link[=xl_page_setup]{xl_page_setup()}},
\code{\link[=xl_properties]{xl_properties()}},
\code{\link[=xl_rich_run]{xl_rich_run()}},
\code{\link[=xl_rich_string]{xl_rich_string()}},
\code{\link[=xl_sheet]{xl_sheet()}},
\code{\link[=xl_sheet_view]{xl_sheet_view()}},
\code{\link[=xl_table]{xl_table()}},
\code{\link[=xl_validation]{xl_validation()}},
\code{\link[=xl_workbook]{xl_workbook()}}
}
\concept{writexl}
