% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xl_outline.R
\name{xl_outline}
\alias{xl_outline}
\title{Control how outline (grouping) symbols are drawn}
\usage{
xl_outline(
  visible = TRUE,
  symbols_below = TRUE,
  symbols_right = TRUE,
  auto_style = FALSE
)
}
\arguments{
\item{visible}{Show the outline symbols at all.  `FALSE` keeps the grouping
(and so the collapsing) but hides the +/- controls.}

\item{symbols_below}{Put the summary row *below* the detail rows, which is
Excel's default.  `FALSE` puts it above.}

\item{symbols_right}{Put the summary column to the *right* of the detail
columns, Excel's default.  `FALSE` puts it to the left.}

\item{auto_style}{Apply Excel's automatic outline styling to the grouped
rows and columns.}
}
\value{
An `xl_outline` object, for [xl_sheet()]'s `outline` argument.
}
\description{
Grouping itself comes from `level` in [xl_col_spec()] / [xl_row_spec()].
`xl_outline()` only changes how the controls are *displayed*, which is
rarely needed --- the defaults match Excel's own.
}
\examples{
xl_outline(symbols_below = FALSE)
xl_outline(visible = FALSE)
}
\seealso{
[xl_sheet], [xl_colrow_spec]

Other worksheet layout:
\code{\link{xl_colrow_spec}},
\code{\link[=xl_page_setup]{xl_page_setup()}},
\code{\link[=xl_sheet]{xl_sheet()}},
\code{\link[=xl_sheet_view]{xl_sheet_view()}}
}
\concept{worksheet layout}
