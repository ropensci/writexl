% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xl_chart.R
\name{xl_chart}
\alias{xl_chart}
\title{Add a chart to a worksheet}
\usage{
xl_chart(
  type,
  series,
  title = NULL,
  title_format = NULL,
  x_axis = NULL,
  y_axis = NULL,
  at = "A1",
  scale = 1,
  offset = NULL,
  position = "move_and_size",
  description = NULL,
  decorative = FALSE,
  style = NA
)
}
\arguments{
\item{type}{The chart type: `"column"`, `"bar"`, `"line"`, `"pie"`,
`"doughnut"`, `"area"`, `"scatter"`, `"radar"`, and the stacked,
percent-stacked, smoothed and marker variants.}

\item{series}{One [xl_chart_series()], or a list of them.  Every series of a
scatter chart must have `categories`, which are its x axis.}

\item{title}{The chart title.  A string is always taken literally, so to
take the title from a cell give a range spec ---
`list(header = "revenue")` for a column's header cell, or
`list(rows = 1, cols = 1)` for a data cell.  `FALSE` removes the title
Excel would otherwise generate.}

\item{title_format}{An [xl_format()] styling the title text.  A title is
text, so only the [xl_font()] group applies.}

\item{x_axis, y_axis}{An [xl_chart_axis()] describing that axis.  Pie and
doughnut charts have none, and several axis options apply to a value or a
category axis only --- see [xl_chart_axis()].}

\item{at}{The cell the chart's top-left corner is anchored to.}

\item{scale}{Scale factor: one number for both axes, or `c(x, y)`.}

\item{offset}{Offset from the anchor cell's corner in pixels, as `c(x, y)`.}

\item{position}{How the chart behaves when rows and columns change size; see
[xl_image()].}

\item{description}{Alt text, for screen readers.}

\item{decorative}{Mark the chart as decorative, so screen readers skip it.}

\item{style}{Excel's built-in chart style, 1--48.}
}
\value{
An `xl_chart` object.
}
\description{
`xl_chart()` builds a chart from one or more [xl_chart_series()] and places
it on a sheet, anchored to a cell.  Pass one or a list of them as
`xl_sheet(chart = )`.

Placement works exactly as it does for [xl_image()] --- `at`, `scale`,
`offset`, `position`, `description` and `decorative` mean the same things,
because libxlsxwriter describes both with the same fields.
}
\section{What a chart type supports}{

Excel silently drops options a chart type cannot use, so writexl refuses them
instead, naming the types that would work.  Pie and doughnut charts have no
axes; only a doughnut has a hole; only pie and doughnut rotate; up-down bars
and high-low lines are line-only; the series gap and overlap are bar and
column only.
}

\examples{
xl_chart("column", xl_chart_series(values = list(cols = "revenue")))
xl_chart("pie", xl_chart_series(values = "Data!B2:B5"), title = "Share")
}
\seealso{
[xl_chart_series], [xl_sheet]

Other images and charts:
\code{\link[=xl_chart_axis]{xl_chart_axis()}},
\code{\link[=xl_chart_error_bars]{xl_chart_error_bars()}},
\code{\link[=xl_chart_labels]{xl_chart_labels()}},
\code{\link[=xl_chart_marker]{xl_chart_marker()}},
\code{\link[=xl_chart_series]{xl_chart_series()}},
\code{\link[=xl_chart_trendline]{xl_chart_trendline()}},
\code{\link[=xl_image]{xl_image()}}
}
\concept{images and charts}
