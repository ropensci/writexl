% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xl_filter.R
\name{xl_filter}
\alias{xl_filter}
\title{Filter an autofilter column, hiding the rows that do not match}
\usage{
xl_filter(
  col,
  criteria = NA,
  value = NULL,
  criteria2 = NA,
  value2 = NULL,
  and_or = "and",
  list = NULL
)
}
\arguments{
\item{col}{The column to filter: a name or a 1-based position.}

\item{criteria}{One of `"=="`, `"!="`, `">"`, `"<"`, `">="`, `"<="`,
`"blanks"` or `"non-blanks"`.  The four magnitude comparisons need a
numeric, `Date` or `POSIXct` column.}

\item{value}{The value to compare against.  For text columns `*` matches any
run of characters and `?` any single one.}

\item{criteria2, value2}{An optional second rule for the same column.}

\item{and_or}{How the two rules combine: `"and"` (default) or `"or"`.}

\item{list}{Instead of a criteria, keep only rows whose value is in this
character vector.  Matched case-insensitively, as Excel does.}
}
\value{
An `xl_filter` object.
}
\description{
`xl_filter()` sets the criteria on one autofilter column *and* hides the rows
that do not match.  Both halves are necessary: Excel stores the criteria and
the hidden rows separately and does not apply a filter when a file is opened,
so criteria alone produce a sheet that looks filtered but shows every row.

Because writexl decides which rows to hide, it reproduces Excel's own
matching rules: text comparison is case-insensitive, `*` and `?` are
wildcards, blank covers both an empty cell and an empty string, and there is
no coercion between text and numbers.
}
\examples{
xl_filter("qty", ">", 100)
xl_filter("qty", ">", 100, "<", 200)          # between, via two rules
xl_filter("fruit", "==", "ap*")               # wildcard
xl_filter("fruit", list = c("apple", "banana"))
}
\seealso{
[xl_sheet]

Other writexl:
\code{\link[=is_xl_comment]{is_xl_comment()}},
\code{\link[=is_xl_format]{is_xl_format()}},
\code{\link[=xl_cell_general]{xl_cell_general()}},
\code{\link[=xl_color]{xl_color()}},
\code{\link{xl_colrow_spec}},
\code{\link[=xl_comment]{xl_comment()}},
\code{\link{xl_conditional}},
\code{\link[=xl_format]{xl_format()}},
\code{\link{xl_format_groups}},
\code{\link[=xl_formula]{xl_formula()}},
\code{\link[=xl_merge]{xl_merge()}},
\code{\link[=xl_page_setup]{xl_page_setup()}},
\code{\link[=xl_properties]{xl_properties()}},
\code{\link[=xl_rich_run]{xl_rich_run()}},
\code{\link[=xl_rich_string]{xl_rich_string()}},
\code{\link[=xl_sheet]{xl_sheet()}},
\code{\link[=xl_sheet_view]{xl_sheet_view()}},
\code{\link[=xl_validation]{xl_validation()}},
\code{\link[=xl_workbook]{xl_workbook()}}
}
\concept{writexl}
