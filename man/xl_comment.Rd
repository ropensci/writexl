% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xl_comment.R
\name{xl_comment}
\alias{xl_comment}
\title{Create a cell comment}
\usage{
xl_comment(
  text,
  format = NULL,
  author = NA,
  visible = NA,
  width = NA,
  height = NA,
  x_scale = NA,
  y_scale = NA,
  start_row = NA,
  start_col = NA,
  x_offset = NA,
  y_offset = NA
)
}
\arguments{
\item{text}{A single string: the comment text.}

\item{format}{An optional [xl_format]; only its fill background color and
font name/size/family are used (see [xl_fill()], [xl_font()]).  Other
properties are unsupported and warned about.}

\item{author}{Comment author (shown in Excel's status bar).  Defaults to the
sheet/workbook `comment_author` when unset (see [xl_sheet()],
[xl_properties()]).}

\item{visible}{Initial visibility: `NA` follows the sheet default (comments
are hidden unless `show_comments` is set), `TRUE` shows this comment,
`FALSE` hides it.}

\item{width, height}{Comment box size in pixels (defaults 128 x 74).}

\item{x_scale, y_scale}{Box scale factors.}

\item{start_row, start_col}{Zero-based anchor cell of the box (by default a
comment sits one row up and one column right of its cell).}

\item{x_offset, y_offset}{Pixel offset of the box from its anchor.}
}
\value{
An `xl_comment` object.
}
\description{
`xl_comment()` builds a comment (note) that can be attached to a cell via
[xl_cell_general()]'s `comment` argument.  The simplest form is just text
(`xl_cell_general(value = x, comment = "see note")`); `xl_comment()` adds
options such as the author, initial visibility, box size and position, and
styling.

Excel comment boxes support only a **background color** and a **font
name/size/family**.  These are supplied by reusing the formatting engine via
the `format` argument (e.g. `xl_font(name = "Arial", size = 10) +
xl_fill(background = "lightyellow")`); any other format property (bold,
borders, number formats, ...) is not supported by comments and triggers a
warning.
}
\examples{
# plain text
xl_comment("Double-check this figure")

# with author and styling reused from the format engine
xl_comment("Estimate", author = "Finance",
           format = xl_font(name = "Arial", size = 10) +
                    xl_fill(background = "lightyellow"))
}
\seealso{
[xl_cell_general], [xl_format]

Other writexl:
\code{\link[=is_xl_comment]{is_xl_comment()}},
\code{\link[=is_xl_format]{is_xl_format()}},
\code{\link[=xl_cell_general]{xl_cell_general()}},
\code{\link[=xl_color]{xl_color()}},
\code{\link{xl_colrow_spec}},
\code{\link{xl_conditional}},
\code{\link[=xl_filter]{xl_filter()}},
\code{\link[=xl_filter_keep]{xl_filter_keep()}},
\code{\link[=xl_format]{xl_format()}},
\code{\link{xl_format_groups}},
\code{\link[=xl_formula]{xl_formula()}},
\code{\link[=xl_merge]{xl_merge()}},
\code{\link[=xl_outline]{xl_outline()}},
\code{\link[=xl_page_setup]{xl_page_setup()}},
\code{\link[=xl_properties]{xl_properties()}},
\code{\link[=xl_rich_run]{xl_rich_run()}},
\code{\link[=xl_rich_string]{xl_rich_string()}},
\code{\link[=xl_sheet]{xl_sheet()}},
\code{\link[=xl_sheet_view]{xl_sheet_view()}},
\code{\link[=xl_table]{xl_table()}},
\code{\link[=xl_table_column]{xl_table_column()}},
\code{\link[=xl_validation]{xl_validation()}},
\code{\link[=xl_workbook]{xl_workbook()}}
}
\concept{writexl}
