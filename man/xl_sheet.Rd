% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xl_sheet.R
\name{xl_sheet}
\alias{xl_sheet}
\title{A worksheet with formatting and layout options}
\usage{
xl_sheet(
  data,
  cols = NULL,
  rows = NULL,
  freeze = NULL,
  gridlines = NA,
  tab_color = NA,
  zoom = NA,
  default_row_height = NA,
  autofilter = FALSE,
  protect = FALSE
)
}
\arguments{
\item{data}{A data frame (the sheet contents).}

\item{cols}{An [xl_col_spec()], or a list of them.}

\item{rows}{An [xl_row_spec()], or a list of them.}

\item{freeze}{Frozen panes: an Excel cell reference such as `"A2"` (freeze
the rows above and columns left of that cell), or `list(row =, col =)`
giving the number of rows/columns to freeze.}

\item{gridlines}{Logical; show (`TRUE`) or hide (`FALSE`) screen gridlines.
`NA` leaves Excel's default.}

\item{tab_color}{Sheet tab color (see [xl_color]).}

\item{zoom}{Zoom level as a percentage (10--400).}

\item{default_row_height}{Default height (in points) for rows in the sheet.}

\item{autofilter}{Add an autofilter (filter dropdowns). `TRUE` covers the
whole used range (header plus data); an Excel range string such as
`"A1:D51"` restricts it; `FALSE` (default) adds none.}

\item{protect}{Protect the worksheet. `TRUE` enables Excel's default
protection, a string sets a password, and a list gives fine-grained
control: `list(password = "secret", format_cells = TRUE, ...)`. The option
names mirror libxlsxwriter's `lxw_protection` struct
(`format_cells`, `format_columns`, `format_rows`, `insert_columns`,
`insert_rows`, `insert_hyperlinks`, `delete_columns`, `delete_rows`,
`sort`, `autofilter`, `pivot_tables`, `scenarios`, `objects`,
`no_select_locked_cells`, `no_select_unlocked_cells`). Setting one of the
editing options to `TRUE` *allows* that action in the protected sheet
(e.g. `format_cells = TRUE` lets users format cells); the `no_select_*`
options instead disable selection. `FALSE` (default) leaves the sheet
unprotected. Cell locking via [xl_protection()] only takes effect when the
sheet is protected.}
}
\value{
An `xl_sheet` object.
}
\description{
`xl_sheet()` wraps a data frame together with worksheet-level options
(column/row formatting and geometry, frozen panes, gridlines, tab color,
zoom).  Pass it anywhere [write_xlsx()] accepts a data frame; a plain data
frame continues to behave exactly as before.
}
\examples{
df <- data.frame(name = c("a", "b"), revenue = c(1000.5, 2000.25))
sheet <- xl_sheet(
  df,
  cols   = xl_col_spec("revenue", width = 14, format = xl_num_format("#,##0.00")),
  freeze = "A2",
  tab_color = "steelblue"
)
tmp <- write_xlsx(list(Data = sheet))
}
\seealso{
[xl_col_spec], [xl_row_spec], [write_xlsx]

Other writexl:
\code{\link[=is_xl_format]{is_xl_format()}},
\code{\link[=xl_cell_general]{xl_cell_general()}},
\code{\link[=xl_color]{xl_color()}},
\code{\link{xl_colrow_spec}},
\code{\link[=xl_format]{xl_format()}},
\code{\link{xl_format_groups}},
\code{\link[=xl_formula]{xl_formula()}},
\code{\link[=xl_properties]{xl_properties()}},
\code{\link[=xl_workbook]{xl_workbook()}}
}
\concept{writexl}
